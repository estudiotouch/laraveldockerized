Laravel Dockerized
---

Projeto base para trabalhar com Laravel usando Docker


## Pre-requisitos

 * Docker

## Inicialização

 - Execute `# ./build.sh`
 - Na primeira vez que executar o Composer (ou quando necessário), use o arquivo `./install_composer.sh`